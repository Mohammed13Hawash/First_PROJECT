specVersion: "v1beta"
serviceId: "work"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "work-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
